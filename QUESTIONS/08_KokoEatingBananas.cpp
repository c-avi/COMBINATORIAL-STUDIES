#include <iostream>
using namespace std;

int main(){
    int n, h;
    int arr[n];
    cout << "Enter no of pile: ";
    cin >> n;
    cout << "Enter piles values: ";
    for (int i = 0; i < n; ++i){
        cin >> arr[i];
    }
    cout << "Enter h: ";
    cin >> h;
    koko()
    return 0;
}